import { EquipoDetalle } from "@/components/EquipoId/EquipoDetalle";
import { Grid } from "@mui/material";

export default function EquipoId() {
    return (
        <Grid item container>
            <EquipoDetalle/>
        </Grid>
    );
}